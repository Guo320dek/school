[build]
  builder = "nixpacks"
  buildCommand = "node node_modules/vite/bin/vite.js build"

[deploy]
  startCommand = "node server/index.cjs"

[service]
  [service.volume]
    name = "data"
    mountPath = "/data"
