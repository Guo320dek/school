[build]
  builder = "nixpacks"
  buildCommand = "echo 'Build skipped - dist committed'"

[deploy]
  startCommand = "node server/debug.cjs"

[service]
  [service.volume]
    name = "data"
    mountPath = "/data"
