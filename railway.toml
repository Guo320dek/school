[build]
  builder = "nixpacks"
  buildCommand = "echo 'Build skipped - dist committed'"

[deploy]
  startCommand = "node server/index.cjs"

[service]
  [service.volume]
    name = "data"
    mountPath = "/data"

[variables]
  NIXPACKS_APT_PACKAGES = "python3, gcc, g++, make"
